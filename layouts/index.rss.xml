{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" 
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Site.BaseURL }}</link>
    <description>Recent content on {{ .Site.Title }}</description>
    <atom:link href="{{ .Permalink }}index.xml" rel="self" type="application/rss+xml" />
    
    {{ range first 20 (where .Site.RegularPages "Section" "posts") }}
    {{ if not .Params.noSitemap }}
    <item>
      <!-- 🚨 Wrapped Title in CDATA to protect special characters -->
      <title><![CDATA[{{ .Title }}]]></title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      
      <!-- 🚨 Wrapped Categories/Tags in CDATA -->
      {{ with .Params.categories }}{{ range . }}<category><![CDATA[{{ . }}]]></category>{{ end }}{{ end }}
      {{ with .Params.tags }}{{ range . }}<category><![CDATA[{{ . }}]]></category>{{ end }}{{ end }}

      {{/* 🖼️ FEATURED IMAGES */}}
      {{ $imgStr := .Params.thumbnail | default (index .Params.images 0) }}
      {{ if $imgStr }}
        {{ $res := resources.Get $imgStr }}
        {{ if $res }}
          {{ $optimized := $res.Resize "800x webp q80" }}
          <media:content url="{{ $optimized.Permalink }}" medium="image" type="image/webp" />
          <enclosure url="{{ $optimized.Permalink }}" type="image/webp" length="0" />
        {{ else }}
          <media:content url="{{ $imgStr | absURL }}" medium="image" />
          <enclosure url="{{ $imgStr | absURL }}" type="image/jpeg" length="0" />
        {{ end }}
      {{ end }}

      {{/* 📝 FULL ENCODED CONTENT (Already in CDATA, but added nbsp scrubber just in case) */}}
      <content:encoded>
        <![CDATA[
          {{ if $imgStr }}
            {{ $res := resources.Get $imgStr }}
            {{ if $res }}
              <p><img src="{{ ($res.Resize "800x webp q80").Permalink }}" alt="{{ .Title }}" /></p>
            {{ else }}
              <p><img src="{{ $imgStr | absURL }}" alt="{{ .Title }}" /></p>
            {{ end }}
          {{ end }}
          {{ .Content | replaceRE "&nbsp;" " " | safeHTML }}
        ]]>
      </content:encoded>
      
      <!-- 🚨 Wrapped Description in CDATA and forcibly scrubbed nbsp -->
      <description><![CDATA[{{ .Summary | plainify | replaceRE "&nbsp;" " " | safeHTML }}]]></description>
    </item>
    {{ end }}
    {{ end }}
  </channel>
</rss>